all the python files can be run like this
python filename.py
e.g. the command
python MC_mass_slow.py
should make a plot

it's possible you'll need to download/import libraries
TODO: look into writing a script to do this so things just work

the one bash script (.sh) will run if you type
. light.sh
or
source light.sh
